name: "DBUS API (Donostia)"
version: "1.0.0"
slug: "dbus_api"
description: "Backend REST no oficial con los tiempos de espera de autobús de DBUS Donostia (dbus.eus), para la integración dbus_donostia"
url: "https://github.com/tu-usuario-github/ha-dbus-addons"
arch:
  - amd64
  - aarch64
  - armv7
init: false
startup: application
boot: auto
host_network: false
ports:
  8080/tcp: 8080
ports_description:
  8080/tcp: "API HTTP (usa esta URL/puerto en la integración dbus_donostia)"
map: []
